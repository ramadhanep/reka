# REKA environment example.
# Copy to .env and adjust. Never commit real secrets.

# Application
APP_ENV=development
API_PORT=4000
WEB_PORT=3000

# Database (local Docker PostgreSQL, see infra/compose/compose.yaml)
DATABASE_URL=postgres://reka:reka@localhost:5432/reka